let addBtn = document.querySelectorAll('.bi-cart-fill');
let card = document.querySelectorAll('.card')
let data = [];

addBtn.forEach((btn,index)=>{
    btn.addEventListener("click",()=>{
        let title = card [index].querySelector(".card-title")
    })
})